
// Data will now be fetched from public/data/flashcards.json and public/data/mcqs.json
// This file can be used for other constants if needed in the future.

// Ensure types are still available if other parts of the app might need them directly,
// though App.tsx will primarily use types for the fetched data.
export {}; // Ensures this file is treated as a module.
